initSidebarItems({"struct":[["ParseFn","Parse function wrapper `use psc::{Stream, ParseMsg, satisfy, ParseFn, Parser, pure}; fn parse_fn<S: Stream<Item = char> + Clone>(stream: &mut S) -> Result<(), ParseMsg> {        let parser =         (satisfy(|ch: &char| ch.is_uppercase()).wrap() >> ParseFn(parse_fn))        | satisfy(|ch: &char| ch.is_lowercase()).wrap() >> pure(|| {});        parser.parse(stream)    }`"],["ParserWrapper","Alg wrapper 1. `pa.wrap() >> pb ~ pa.and_r(pb)` 2. `pa.wrap() << pb ~ pa.and_l(pb)` 3. `pa.wrap() | pb ~ pa.or(pb)`"]]});